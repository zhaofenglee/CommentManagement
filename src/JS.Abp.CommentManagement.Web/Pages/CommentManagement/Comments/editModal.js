var abp = abp || {};

abp.modals.commentEdit = function () {
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("CommentManagement");
        
        
        
        
        
    };

    return {
        initModal: initModal
    };
};
